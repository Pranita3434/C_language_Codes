#include<void>

int main(void)
{
    enum BOOL ret;
    ret = is_prime(no);
 
    if(ret==true)
    {
        printf("Num Is Prime");
    }
    else
        pritnf("Num Is Not Prime");

    return 0;
}
enum BOOL is_prime(int no)
{ 
    if()
       return true;
    else
       return false;
}

